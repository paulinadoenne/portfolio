# Hinweise für Claude

- Das Projekt ist über Vercel deployed. Nach jeder Änderung automatisch committen und pushen (kein Nachfragen nötig).
- Immer direkt auf den Branch `main` pushen (nicht auf einen Feature-Branch).
