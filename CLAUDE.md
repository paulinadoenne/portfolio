# Hinweise für Claude

- Das Projekt ist über Vercel deployed. Nach jeder Änderung automatisch committen und pushen (kein Nachfragen nötig).
- Immer direkt auf den Branch `main` pushen (nicht auf einen Feature-Branch).
- In Website-Texten (Titel, Beschreibungen, Captions, Metadaten) keine Gedankenstriche (—) verwenden. Stattdessen Komma, Doppelpunkt, Klammern oder `·` (wie an anderen Stellen der Seite üblich). Code-Kommentare sind davon nicht betroffen.
